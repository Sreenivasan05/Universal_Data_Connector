version: '3.9'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env                      
    environment:
      - LOG_JSON=true             
      - ENVIRONMENT=production
    volumes:
      - ./data:/app/data          
    restart: unless-stopped       
